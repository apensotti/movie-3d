import { omdb } from '@/data/types';
import { getRecommendations } from '@/hooks/searchHooks';
import React from 'react'

const MovieRecommendations = (movies:omdb[]) => {

    return (
      <div>MovieRecommendations</div>
    )
}

export default MovieRecommendations